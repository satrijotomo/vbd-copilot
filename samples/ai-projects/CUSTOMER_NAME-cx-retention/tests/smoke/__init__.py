"""Smoke tests for the CUSTOMER_NAME Bill Explainer live deployment.

These tests verify that deployed endpoints respond correctly.
They are gated behind the --live pytest flag and require the
API_BASE_URL environment variable to point at the target deployment.
"""
